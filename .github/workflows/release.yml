name: Release

on:
  release:
    types: [published]

jobs:
  release:
    name: Prepare release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      # Build
      - name: Build the file
        run: |
          cd /home/runner/work/nissan-leaf-card/nissan-leaf-card
          npm install
          npm run build

      # Upload build file to the releas as an asset.
      - name: Upload zip to release
        uses: svenstaro/upload-release-action@v1-release

        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: /home/runner/work/nissan-leaf-card/nissan-leaf-card/dist/nissan-leaf-card.js
          asset_name: nissan-leaf-card.js
          tag: ${{ github.ref }}
          overwrite: true